8 kyu
Array plus array
Dart:

int arrayPlusArray(List<int> arr1, List<int> arr2) {
  int suma = arr1.fold(0, (acum, num) => acum + num);
  suma+= arr2.fold(0,(acum , num) =>acum + num);
  return suma; //something went wrong
}
